import React, {FunctionComponent} from 'react'
import {TableComponent} from '../../../components/widgets/TableWidget/TableWidget'


export const Table: FunctionComponent<TableComponent> = (props) => {
    // const {columns, data} = props
    return <div>

    </div>
}

export default Table